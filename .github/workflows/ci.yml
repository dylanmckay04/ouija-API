name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5

      - name: Set up Python 3.13
        run: uv python install 3.13

      - name: Install dependencies
        working-directory: backend
        run: uv sync

      - name: Run tests
        timeout-minutes: 5
        working-directory: backend
        env:
          TESTING: "1"
          SECRET_KEY: ${{ secrets.SECRET_KEY }}
          DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/static_test"
          REDIS_URL: "redis://localhost:6379"
        run: TESTING=1 uv run pytest -v --timeout=60
